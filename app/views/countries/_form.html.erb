<%= form_with(model: country) do |form| %>
  <% if country.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(country.errors.count, "error") %> prohibited this country from being saved:</h2>

      <ul>
        <% country.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <%= country_select("rr", "country", { include_blank: 'Select a country' }, { class: 'country-select-box' }) %>

  <%= country_select("ss", "country", priority_countries: ["GB", "FR", "DE"], sort_provided: false) %>
  <%#= form.input :country_field, collection: CountryStateSelect.countries_collection %>


  <div>
    <%= form.submit %>
  </div>
<% end %>
